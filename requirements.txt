# Dependencias del proyecto

# Django para interfaz web moderna
Django>=4.2.0

# Librerías estándar de Python (incluidas):
# - socket (comunicación de red)
# - threading (hilos)
# - json (serialización)
# - base64 (codificación)
# - os, pathlib (sistema de archivos)
# - hashlib (hashes)
# - datetime (fechas)
# - dataclasses (estructuras de datos)
# - enum (enumeraciones)

# Para instalar:
# pip install -r requirements.txt

